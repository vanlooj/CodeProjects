# This function converts inches to centimeters
def convertInchesToCentimeters(inches):

    # Performs calculation to convert inches to centimeters
    centimeters = inches * 2.54
    print(centimeters)